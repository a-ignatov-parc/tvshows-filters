{
  "name": "tvshows-filters",
  "version": "1.0.0",
  "description": "",
  "main": "src",
  "engine-strict": true,
  "engines": {
    "node": "^8.9.1",
    "yarn": "^1.3.0"
  },
  "scripts": {
    "install-deps": "npx yarn@$npm_package_engines_yarn install --frozen-lockfile --check-files",
    "build": "rm -rf dist && mkdir -p dist && node src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/a-ignatov-parc/tvshows-filters.git"
  },
  "author": "Anton Ignatov <abietis@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/a-ignatov-parc/tvshows-filters/issues"
  },
  "homepage": "https://github.com/a-ignatov-parc/tvshows-filters#readme",
  "dependencies": {
    "listr": "^0.14.1",
    "node-fetch": "^2.1.2"
  }
}
